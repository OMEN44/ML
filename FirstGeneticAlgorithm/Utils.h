#pragma once
#ifndef UTILS_H
#define UTILS_H

class Utils {
public:
	Utils();
	float randF(float min, float max);
	float randF(float max);
	int randI(int min, int max);
	int randI(int max);
};

#endif // !UTILS_H
